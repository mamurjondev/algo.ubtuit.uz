#include <bits/stdc++.h>
#define _sf(x) setprecision(x) << fixed
typedef long int li;
typedef long long ll;
typedef long double ld;
using namespace std;
int main()
{
	int a, b, c;
	cin >> a >> b >> c;
	float yp;
	yp = (a + b + c) / 2.;
	cout << setprecision(2) << fixed << yp << '\n';
}
